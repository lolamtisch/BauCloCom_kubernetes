namespace: nextcloud

commonLabels:
  app: nextcloud
  version: "15"

bases:
  - minio
  - redis
  - mySQL
  - nextcloud
  - cron

patchesStrategicMerge:
  - patch.yaml

vars:
  - name: MYSQL_SERVICE
    objref:
      apiVersion: v1
      kind: Service
      name: mysql
  - name: REDIS_SERVICE
    objref:
      apiVersion: v1
      kind: Service
      name: redis
